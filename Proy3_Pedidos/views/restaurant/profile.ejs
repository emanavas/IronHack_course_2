<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Restaurant Profile - FoodExpress</title>
    <link rel="stylesheet" type="text/css" href="/style.css" />
    <link rel="stylesheet" type="text/css" href="/lib_style.css" />
    <!-- Add favicon-->
    <link rel="icon" type="image/x-icon" href="/assets/favicon.ico" />
    <!--bootstrap-->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
  </head>
  <body>
    <main>
      <!-- Navbar -->
      <%- include('../partials/navbar') %>

      <section class="py-5 bg-light">
        <div class="container">
          <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>My Restaurant Profile</h1>
            <a href="/restaurant/edit" class="btn btn-danger">
              <i class="fas fa-edit me-2"></i>Edit Profile
            </a>
          </div>

          <% if (error && error.length > 0) { %>
            <div class="alert alert-danger"><%= error %></div>
          <% } %>
          <% if (success && success.length > 0) { %>
            <div class="alert alert-success"><%= success %></div>
          <% } %>

          <div class="card">
            <div class="card-body p-4">
              <div class="row">
                <div class="col-md-8">
                  <h2 class="card-title mb-3"><%= restaurant.name %></h2>
                  <p class="text-muted"><%= restaurant.cuisine %></p>
                  <p><%= restaurant.description %></p>
                </div>
                <div class="col-md-4 text-md-end">
                  <!-- Placeholder for an image -->
                  <img src="https://via.placeholder.com/200x150" class="img-fluid rounded" alt="Restaurant Image">
                </div>
              </div>

              <hr class="my-4" />

              <h4 class="mb-3">Details</h4>
              <div class="row">
                <div class="col-md-6">
                  <p><strong><i class="fas fa-map-marker-alt me-2"></i>Address:</strong> <%= restaurant.address %></p>
                  <p><strong><i class="fas fa-phone me-2"></i>Phone Number:</strong> <%= restaurant.phoneNumber %></p>
                </div>
                <div class="col-md-6">
                  <p><strong><i class="fas fa-clock me-2"></i>Opening Hours:</strong> <%= restaurant.openingHours %></p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Footer -->
      <%- include('../partials/footer') %>
    </main>
  </body>
</html>