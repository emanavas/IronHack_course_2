<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cursos Disponibles</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="d-flex flex-column min-vh-100">
  <%- include('partials/navbar') %>

  <main class="container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="mb-0">Cursos Disponibles</h1>
        <a href="/courses/new" class="btn btn-success">Añadir Curso</a>
    </div>

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
      <% if (locals?.courses && locals?.courses.length > 0) { %>
        <% courses.forEach(course => { %>
          <div class="col">
            <div class="card h-100">
              <div class="card-body">
                <h5 class="card-title"><%= course.title %></h5>
                <p class="card-text"><%= course.description %></p>
                <span class="badge bg-info text-dark"><%= course.category %></span>
              </div>
              <div class="card-footer d-flex justify-content-end">
                <a href="/courses/edit/<%= course._id %>" class="btn btn-primary btn-sm me-2">Editar</a>
                <form action="/courses/delete/<%= course._id %>?_method=DELETE" method="POST">
                  <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                </form>
              </div>
            </div>
          </div> 
              
        <% }) %>
      <% } else { %>
        <div class="col">
          <p>No hay cursos disponibles en este momento.</p>
        </div>
      <% } %>
    </div>
  </main>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
